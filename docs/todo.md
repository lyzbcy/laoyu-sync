# To-do / 开发情况

## 当前版本：0.1.0（2026-09-07）

v0.1.0 已完成：
- 核心服务：Syncthing 进程守护、状态聚合（文件夹进度/设备连接/传输速度）、活动事件流、双通道日志
- 添加设备向导：我的设备二维码、待确认设备一键接受、按 ID 添加并勾选共享文件夹
- 添加同步文件夹（手动选路径）
- 网页 UI：仪表盘 / 设备与配对 / 同步文件夹 / 动态 / 关于（含推广页与反馈入口）
- pywebview 窗口壳（缺依赖时回退浏览器打开）
- 版本号 + 自适应更新检测框架（更新源未配置时静默跳过）
- 桌面小精灵插件接入管理界面菜单

## To-do（按优先级）

1. **配对体验升级**（v0.2）：`syncthing://` 链接协议注册；两边都装捞鱼同步小助手时用确认码自动互加；扫描二维码添加。
2. **失败文件产品化**（v0.2）：拉取失败列表（如 Windows 保留名 nul），提供一键重命名/忽略建议。
3. **首次安装向导**（v0.2）：选一个文件夹开始同步 → 引导添加设备。
4. **打包发布**（v0.3）：Inno Setup（Win）+ app bundle（Mac），捆绑 syncthing.exe，见 docs/reference/packaging.md。
5. **一键更新落地**（v0.3）：等 GitHub 仓库建立后配置 update_manifest_url，做下载进度条 + 代理提醒 + 失败兜底。
6. **求好评弹窗**（v0.3）：中度用户判断 + 15 天静默逻辑。
7. 回归测试接入 Kimi WebBridge 子代理流程（见用户规范《网页回归测试》）。
8. 向导第三形态：扫描对方二维码添加（v0.1 仅支持复制 ID / 待确认列表）。

## 开发记录

- 2026-09-07 v0.1.0：项目创建，核心服务 + 添加设备向导 + UI 五页 + 小精灵接入。
  - 实测通过：仪表盘实时状态（100%/v2.1.3/双设备）、添加设备→共享文件夹→自动重启处理→移除全链路（用 `syncthing generate` 生成合法测试 ID）、非法路径校验、动态事件流、关于页、401 鉴权。
  - 浏览器回归中发现并修复：app.js TDZ 启动崩溃（boot 提前调用）、设备 ID 误判 7 组（实为 8 组 56 位）、Syncthing 400 错误信息未透传、pythonw 下 tasklist 闪控制台窗口（改为仅 API 不可达时查进程 + CREATE_NO_WINDOW）。
  - 小精灵右键菜单新增「打开捞鱼同步小助手管理界面」（带令牌直达）。

## v0.2.0 交付记录（2026-09-07）

- 产品改名「捞鱼同步小助手」，仓库：https://github.com/lyzbcy/laoyu-sync
- 同步流程项目制改造完成并双实例实测：对端分享「工作」→ 本机待接收列表 → 选位置接收 → 文件到达 ✓
- 三路对抗性评审（视觉 5.5 / 小白用户 5 / 工程师 6.5）整改落地，清水绿视觉体系上线
- 小精灵换新形象：会摆尾吐泡的小蓝鱼（土豆），CSS 全站换肤（田）
- 更新源已接线 raw.githubusercontent.com/lyzbcy/laoyu-sync/main/version.json；git 推送走本机代理 127.0.0.1:7890（已写入仓库 git config）
